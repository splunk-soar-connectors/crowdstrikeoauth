**Unreleased**

* Breaking change: remove deprecated Detects API actions (`list detections` -> `list epp alerts`, `get detections details` -> `get epp details`, `update detections` -> `update epp alerts`, `set status` -> `resolve epp alerts`)
* Clean up unused Detects HTML view templates and view routing tied to removed actions
